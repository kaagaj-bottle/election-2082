# PostgreSQL
POSTGRES_DB=election_2082
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# Django
SECRET_KEY=changeme-generate-a-real-secret-key
ALLOWED_HOSTS=YOUR_VPS_IP,localhost
CORS_ALLOWED_ORIGINS=https://your-app.vercel.app

# SSL (set to true after configuring TLS certificates in nginx)
ENABLE_SSL=false
